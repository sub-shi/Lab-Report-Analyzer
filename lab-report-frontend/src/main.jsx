import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(            // Create a root element and render the App component 
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
